const initState = {
    filingStatus: 'single',
    income: 0,
    filingYear: 0,
    incomeYears: [],
    loading: false,
    error: null
}

const rootReducer = (state = initState, action) => {
    return state;
}

export default rootReducer;